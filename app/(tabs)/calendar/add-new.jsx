import { View, Text } from 'react-native'
import React from 'react'

const AddNewEvent = () => {
  return (
    <View>
        {/* this will become a form for adding a new event to the calendar. this will have to integrate with the db. */}
        {/* db config: https://docs.expo.dev/versions/latest/sdk/sqlite/  */}
        <Text>AddNewEvent</Text>
    </View>
  )
}

export default AddNewEvent
